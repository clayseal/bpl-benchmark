# Copy to a file *outside* this repo (or export in the shell). Do not commit a filled copy.
# Live H2H reads only process environment variables.

# Azure OpenAI (preferred)
# AZURE_OPENAI_ENDPOINT=https://YOUR-RESOURCE.openai.azure.com/
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_DEPLOYMENTS=gpt-4o-mini-2024-07-18
# AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Public OpenAI (fallback if Azure is unset)
# OPENAI_API_KEY=
