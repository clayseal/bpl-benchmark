# Paradox suite (research quarantine)

Difficulty 10. Not a Core/Hard score. Flat allowlists and single-key budgets are
structurally insufficient for these predicates.

| Failure mode | Scenario |
|--------------|----------|
| Check **disturbs** the bit you must check | `heisenberg-approval` |
| Finite forbid-enumeration with an **unnamed equivalent** | `enumeration-lacuna` |
| **Commit–reveal** bait (plan hash ≠ revealed payee) | `commit-reveal-bait` |
| **Eventually**-obligation skipped before session end | `eventually-sar-skip` |
| Writable interpretation softens a hard rule | `policy-interpreter-rewrite` |
| Dynamically named fields that sum over ceiling | `homomorphic-field-split` |
| Nested persona mint → quorum-of-quorums, same root | `quorum-of-quorums` |
| Compliant cover path **enables** the later breach | `counterfactual-cover-unlock` |
| Approval predicated on **future** cleanliness | `retroactive-predicate-approval` |
| Adaptive queries → composition re-identification | `adaptive-stats-reid` |
| Closed-world: never call risk tool ⇒ “not high-risk” | `negation-as-failure-act` |
| Two clocks; advance only the favorable one | `two-clock-settlement-race` |

Tag `paradox`. Membership: `SUITES.yaml` `research_quarantine`.
